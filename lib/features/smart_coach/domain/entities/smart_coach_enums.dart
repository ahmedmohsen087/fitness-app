enum SmartCoachViewMode { welcome, activeChat }

enum SmartCoachActionType {
  navigateFoodDetails,
  navigateExercise,
  navigateWorkouts,
  none,
}

enum MessageSender { user, ai, system }

enum SmartCoachMessageStatus { sent, loadingAi, error }
